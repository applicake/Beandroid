<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@drawable/app_bg"
    android:orientation="vertical" >

    <include
        android:layout_width="fill_parent"
        android:layout_height="@dimen/ActionBarHeight"
        layout="@layout/actionbar" >
    </include>

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_weight="1" >

        <Button
            style="@style/HomeButton"
            android:drawableTop="@drawable/ic_es_dashboard"
            android:onClick="onHomeDashboardClick"
            android:text="Dashboard" >
        </Button>

        <Button
            style="@style/HomeButton"
            android:drawableTop="@drawable/ic_es_repositories"
            android:onClick="onHomeRepositoriesClick"
            android:text="Repositories" >
        </Button>
    </LinearLayout>

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_weight="1" >

        <Button
            android:id="@+id/users_button"
            style="@style/HomeButton"
            android:drawableTop="@drawable/ic_es_users"
            android:onClick="onHomeUsersClick"
            android:text="Users" >
        </Button>

        <Button
            android:id="@+id/account_settings_button"
            style="@style/HomeButton"
            android:drawableTop="@drawable/ic_es_settings"
            android:onClick="onHomeSettingsClick"
            android:text="Account settings" >
        </Button>
    </LinearLayout>

</LinearLayout>