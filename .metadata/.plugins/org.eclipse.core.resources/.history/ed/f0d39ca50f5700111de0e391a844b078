package com.applicake.beanstalkclient.activities;

import com.applicake.beanstalkclient.R;

import android.app.Activity;
import android.content.Intent;
import android.os.Bundle;
import android.support.v4.app.Fragment;
import android.view.LayoutInflater;
import android.view.View;
import android.view.ViewGroup;

public class OverallReposReleasesListFragment extends Fragment {
  
  private Intent mIntent;
  private Activity parentActivity;
  private 
  
  @Override
  public void onCreate(Bundle savedInstanceState) {
    super.onCreate(savedInstanceState);
    
    mIntent = getActivity().getIntent();
    parentActivity = getActivity();
  }
  
  @Override
  public View onCreateView(LayoutInflater inflater, ViewGroup container, Bundle savedInstanceState) {
        View root = LayoutInflater.from(parentActivity).inflate(R.layout.repository_deployments_layout, null);
  }
}
